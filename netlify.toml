# Netlify 배포 설정

[build]
  # 배포 시 빈 config.js 생성 → 실제 키는 환경변수로만 관리
  command = "echo \"window.ENV={};\" > config.js"
  publish = "."

[build.environment]
  NODE_VERSION = "18"

[functions]
  directory = "netlify/functions"
[functions."daily-words"]
  timeout = 60